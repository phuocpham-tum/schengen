Given the current component positions on a grid (cell row and column IDs, with the origin at the bottom-left corner as (0, 0)), determine the optimal orientation for each component on the canvas. The goal is to make the subsequent routing phase with A* algorithm more efficient by minimizing congestion, wire crossings, and overall routing complexity.

The component types in the current netlist are provided below:

{{NETLIST_COMPONENT_TYPE}}

The available orientations for each component type are described here:

{
  "pmos": [
    {
      "orientation": 0,
      "gate": { "location": "right" },
      "source": { "location": "top" },
      "drain": { "location": "bottom" },
      "description": "A PMOS symbol with the source terminal on top, the drain terminal at the bottom, and the gate terminal pointing to the right."
    },
    {
      "orientation": 1,
      "gate": { "location": "left" },
      "source": { "location": "top" },
      "drain": { "location": "bottom" },
      "description": "A PMOS symbol with the source terminal on top, the drain terminal at the bottom, and the gate terminal pointing to the left."
    }
  ],

  "pmos_diode": [
    {
      "orientation": 0,
      "gate": { "location": "right" },
      "source": { "location": "top" },
      "drain": { "location": "bottom" },
      "description": "A PMOS symbol with the source terminal on top, the drain terminal at the bottom, and the gate terminal pointing to the right; the drain and gate terminals are connected."
    },
    {
      "orientation": 1,
      "gate": { "location": "left" },
      "source": { "location": "top" },
      "drain": { "location": "bottom" },
      "description": "A PMOS symbol with the source terminal on top, the drain terminal at the bottom, and the gate terminal pointing to the left; the drain and gate terminals are connected."
    }
  ],

  "nmos": [
    {
      "orientation": 0,
      "gate": { "location": "right" },
      "source": { "location": "top" },
      "drain": { "location": "bottom" },
      "description": "An NMOS symbol with the source terminal on top, the drain terminal at the bottom, and the gate terminal pointing to the right."
    },
    {
      "orientation": 1,
      "gate": { "location": "left" },
      "source": { "location": "top" },
      "drain": { "location": "bottom" },
      "description": "An NMOS symbol with the source terminal on top, the drain terminal at the bottom, and the gate terminal pointing to the left."
    }
  ],

  "nmos_diode": [
    {
      "orientation": 0,
      "gate": { "location": "right" },
      "source": { "location": "top" },
      "drain": { "location": "bottom" },
      "description": "An NMOS symbol with the source terminal on top, the drain terminal at the bottom, and the gate terminal pointing to the right; the drain and gate terminals are connected."
    },
    {
      "orientation": 1,
      "gate": { "location": "left" },
      "source": { "location": "top" },
      "drain": { "location": "bottom" },
      "description": "An NMOS symbol with the source terminal on top, the drain terminal at the bottom, and the gate terminal pointing to the left; the drain and gate terminals are connected."
    }
  ],

  "cap": [
    {
      "orientation": 0,
      "pos": { "location": "top" },
      "neg": { "location": "bottom" },
      "description": "A capacitor symbol with the positive terminal on top and the negative terminal at the bottom."
    },
    {
      "orientation": 1,
      "pos": { "location": "bottom" },
      "neg": { "location": "top" },
      "description": "A capacitor symbol with the positive terminal at the bottom and the negative terminal at the top."
    }
  ]
}

Return your answer in the following JSON format, where each key is a component name and the value is the selected orientation ID (ignoring terminal components, starting with "terminal_"):

{
  "component_name": "orientation_id",
  "component_name": "orientation_id"
}


Example:
{
  "m1": 0,
  "m2": 1,
  "c1": 0
}

Reason through the problem step by step before producing the final answer.